self.addEventListener('fetch', function(event) {
    // Requisito para activar el modo App en el móvil
});self.addEventListener('fetch', function(event) {
    // Requisito para activar el modo App en el móvil
});